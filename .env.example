# Copy to .env and fill in. These are PUBLIC (shipped in the app bundle). Never
# put service-role keys or the Anthropic API key in here — those go in Supabase
# Edge Function secrets only.

EXPO_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOi...
